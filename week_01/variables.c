#include <stdio.h>

int main(void) {
    char my_char;
    int height;
    double max_kg_bicep_curl;

    my_char = 'j';
    height = 175;
    max_kg_bicep_curl = 450.5;

    printf("char is: %c an the unerlying int is %d\n", my_char, my_char);
    // printf("%c is %dcm tall and lifts %.2lfkg!\n", my_char, height, max_kg_bicep_curl);
    return 0;
}